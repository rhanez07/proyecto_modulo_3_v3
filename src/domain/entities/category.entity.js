export default class CategoryEntity {
 constructor({ id, name, userId, createdAt, updatedAt }) {
 this.id = id;
 this.name = name;
 this.userId = userId;
 this.createdAt = createdAt;
 this.updatedAt = updatedAt;
 }
}
